# Copy this file to `.env` in the same folder (backend/.env)

# MongoDB — local (Compass: mongodb://localhost:27017/hidden_india)
MONGO_URI=mongodb://localhost:27017/hidden_india

# JWT — generate a long random string for production (never commit real .env)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_ACCESS_HOURS=24

PORT=5000
FLASK_DEBUG=1
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://localhost:3001

# Google OAuth server-side validation (optional — must match frontend VITE_GOOGLE_CLIENT_ID)
GOOGLE_CLIENT_ID=

# Default admin account (created when you run: python seed.py)
ADMIN_EMAIL=admin@hiddenindia.in
ADMIN_PASSWORD=Admin@123
ADMIN_NAME=System Supervisor
